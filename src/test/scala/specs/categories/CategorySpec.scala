import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner
import org.scalatest.FunSuite
import org.scalatest.matchers.ShouldMatchers
import org.scalatest.{ FunSpec, BeforeAndAfterAll, BeforeAndAfterEach, BeforeAndAfter }

/*
 *
 */
class CategorySpec extends FunSpec with ShouldMatchers {
  describe("Product"){
    /*
    trait Product {
      def outl[A,B](pair:Pair[A,B]):A = pair._1
      def outr[A,B](pair:Pair[A,B]):B = pair._2
      def f[C,A](c:C) : A = 
      def split[A,B,C](c:C) : Pair[A,B] = 
    }
    */
  }
}
